<div class="row">
    <div class="col-12 col-sm-10 py-3">
        <h3>Choose a week to view standings</h2>
        <small>This page is a work in progress and may not work correctly currently</small>
    </div>
    <div class="form-group col-6 col-sm-2">
        <select class="form-control week-dropdown mb-3">
            {{#repeat count=38 start=1}}
            <option value="{{@index}}">week {{@index}}</option>
            {{/repeat}}
        </select>
    </div>
</div>
<div class="row justify-content-md-center">
    <div class="col-12 col-sm-10">
        <div class="league-table-container">
        </div>
    </div>
</div>